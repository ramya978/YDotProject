"use strict";import SeriesRegistry from"../../Core/Series/SeriesRegistry.js";const{seriesTypes:{mapline:{prototype:{pointClass:MapLinePoint}}}}=SeriesRegistry;import U from"../../Core/Utilities.js";const{pick:pick,isString:isString,isNumber:isNumber}=U;class FlowMapPoint extends MapLinePoint{isValid(){let i=!(!this.options.to||!this.options.from);return[this.options.to,this.options.from].forEach(function(s){i=!!(i&&s&&(isString(s)||isNumber(pick(s[0],s.lat))&&isNumber(pick(s[1],s.lon))))}),i}}export default FlowMapPoint;