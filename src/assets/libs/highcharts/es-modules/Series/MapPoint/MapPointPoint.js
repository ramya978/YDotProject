"use strict";import SeriesRegistry from"../../Core/Series/SeriesRegistry.js";const{scatter:ScatterSeries}=SeriesRegistry.seriesTypes;import U from"../../Core/Utilities.js";const{isNumber:isNumber}=U;class MapPointPoint extends ScatterSeries.prototype.pointClass{isValid(){return Boolean(this.options.geometry||isNumber(this.x)&&isNumber(this.y)||isNumber(this.options.lon)&&isNumber(this.options.lat))}}export default MapPointPoint;