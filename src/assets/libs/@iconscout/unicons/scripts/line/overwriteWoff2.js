const ttf2woff2=require("ttf2woff2"),path=require("path"),fs=require("fs-plus"),glob=require("glob"),sourcePath=path.join(process.cwd(),`fonts/${process.env.STYLE}/*.ttf`),fontsPath=path.join(process.cwd(),`fonts/${process.env.STYLE}`);glob(sourcePath,(o,s)=>{s.forEach(o=>{const s=`${o.split("/").pop().split(".").shift()}.woff2`;console.log(`Overwriting ${process.env.STYLE}: ${s}`);const t=fs.readFileSync(o);fs.writeFileSync(`${fontsPath}/${s}`,ttf2woff2(t))})});