const{exec:exec}=require("child_process"),axios=require("axios"),path=require("path"),fs=require("fs"),pkg=require("../package.json");exec(`npm view ${pkg.name} version`,async(e,s)=>{const o=s.trim(),i=["solid"];for(let e=0;e<i.length;e++){const s=i[e],n=`https://unicons.iconscout.com/release-pro/v${o}/json/${s}.json`,r=await axios.get(n),t=path.join(process.cwd(),`json/${s}.json`);fs.writeFileSync(t,JSON.stringify(r.data),"utf-8")}});